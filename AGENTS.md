# AGENTS.md

Isaac episodes + recall module extracted from isaac-agent. Keep behavior byte-identical to the source extraction unless a bean explicitly changes it. Follow TDD and run `bb spec`, `bb features`, and `bb ci` before handoff.
